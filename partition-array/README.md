## Array Partition with Minimal Sum Difference (C)

This program splits an array of integers into two parts such that the sums
of the two parts are as close as possible.

The solution is based on dynamic programming and finds a subset whose sum
is closest to half of the total array sum.
